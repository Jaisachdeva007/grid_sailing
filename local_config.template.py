# ============================================================
#  LOCAL CONFIG TEMPLATE — copy this file to local_config.py
#  and fill in the values for THIS machine.
#
#  local_config.py is gitignored and never committed.
#  Each machine has its own copy.
# ============================================================

# Human-readable name shown in Firebase Console under "devices"
DEVICE_NAME = "ASUS-Juliet"          # e.g. "ASUS-Juliet" or "MacBook-Jai"

# Absolute path to the Firebase service account key JSON file.
# Download from: Firebase Console → Project Settings → Service Accounts
#                → Generate new private key
FIREBASE_CREDENTIALS = "/path/to/serviceAccountKey.json"

# Optional: override the database path if you want it in a specific folder
# DB_PATH = "/path/to/experiment.db"
